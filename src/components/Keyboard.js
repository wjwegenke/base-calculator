import React from 'react';
import { View } from 'react-native';
import Key from './Key';

export default function Keyboard(props) {
    return (
        <View>
            Keyboard
        </View>
    );
}